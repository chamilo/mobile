Continuamos Chamilo Mobile 2.x después del batch de autenticación.

Estado esperado:

- Mobile repo: /var/www/chamilo-mobile
- Branch completed: feature/mobile2-auth
- Commit expected: Mobile: Add JWT authentication flow
- Backend repo: /var/www/chamilo2
- Backend branch: feature/mobile-api-current-user
- Backend commit: ba1f018207 API: Add authenticated user profile operation
- Both working trees clean

No reconstruyas scaffold, campus profiles, transport, JWT login, token storage, guards, profile ni logout.

Objetivo único del siguiente batch:

Chat 04 — audit and implement My courses and sessions.

1. Capture current mobile branch/HEAD/status.
2. Capture backend branch/HEAD/status.
3. With a valid JWT and the authenticated `/api/me` ID, collect sanitized runtime payloads for:
   - GET /api/me/courses
   - GET /api/users/{id}/session_subscriptions/past
   - GET /api/users/{id}/session_subscriptions/current
   - GET /api/users/{id}/session_subscriptions/upcoming
4. Record content type, Hydra pagination, direct membership identity, session/course fields, AccessUrl and role behavior.
5. Audit existing `chamilo-lms/assets/vue` course/session services before creating mobile services.
6. Measure whether existing operations are sufficient. Do not implement GAP-002 backend work unless insufficiency is demonstrated.
7. Implement only the mobile courses/sessions list using `AuthenticatedHttpClient`.
8. Include loading, empty, error, retry and last-read cache states.
9. Preserve nullable `sessionId` and do not merge direct/session memberships incorrectly.
10. Do not start course home or announcements.

Out of scope:

- backend composed endpoint without evidence;
- course tools;
- announcements;
- native HTTP;
- secure native storage;
- refresh token;
- Android/iOS.
