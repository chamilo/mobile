Continuamos Chamilo Mobile 2.x después del inicio móvil del curso.

Estado esperado:

- Mobile repo: /var/www/chamilo-mobile
- Completed branch: feature/mobile2-course-home
- Expected commit: Mobile: Add mobile course home
- Backend repo: /var/www/chamilo2
- Backend branch: feature/mobile-api-current-user
- Backend commit: ba1f018207 API: Add authenticated user profile operation
- Both working trees clean

No reconstruyas scaffold, campus profiles, transport, JWT login, profile, logout, courses/sessions, route context, course header or ToolCapability registry.

Objetivo único:

Chat 06 — Read-only announcements.

1. Verify branch, HEAD and clean state.
2. Re-audit the existing announcement list/detail contracts and current Vue announcement services.
3. Consume only:
   - GET /api/announcement/list with cid/sid/gid context;
   - GET /api/announcement/{id} with the same context.
4. Preserve the exact direct or session course route context from Chat 05.
5. Implement list and detail, dates, loading, error, empty, retry and cached-read states.
6. Sanitize server HTML before rendering; do not use raw v-html without an audited sanitizer strategy.
7. Open external links safely.
8. Add attachments only if their download contract is verified.
9. Validate student visibility and session/course ownership with runtime evidence.
10. Update all state and handoff files and provide a full ZIP.

Out of scope:

- create/edit/delete announcements;
- notification writes;
- other course tools;
- GraphQL;
- native HTTP;
- secure native storage;
- Android/iOS.
