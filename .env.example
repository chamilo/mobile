# Optional single-campus development proxy.
# The selected campus URL must match VITE_DEV_PROXY_TARGET exactly after normalization.
VITE_USE_DEV_PROXY=false
VITE_DEV_PROXY_TARGET=https://chamilo2.local
VITE_DEV_PROXY_INSECURE=true
