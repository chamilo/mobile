# PROJECT_STATE

Chat 09 target: Notebook tool on branch `feature/mobile2-notebook`, based on mobile commit `dce0cb7`.

Verified API contracts:

- GET `/api/notebook/list`
- GET `/api/notebook/form`
- POST `/api/notebook`
- PUT `/api/notebook/{iid}`
- DELETE `/api/notebook/{iid}`

Backend changes: none.
